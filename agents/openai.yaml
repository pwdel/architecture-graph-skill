interface:
  display_name: "Architecture Graph"
  short_description: "Build and query architecture evidence graphs"
  default_prompt: "Use $architecture-graph capabilities first, then index selected architecture files and traverse bounded, source-backed terms, graph neighbors, decisions, and explanations."
