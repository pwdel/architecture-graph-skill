interface:
  display_name: "Architecture Graph"
  short_description: "Index and query architecture evidence"
  default_prompt: "Use $architecture-graph to index selected architecture files and return bounded, source-backed evidence."
